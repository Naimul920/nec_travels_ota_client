import SettingsPanel from "../../_components/Settings/SettingsPanel";

export default function EditCurrencyPage() {
  return (
    <SettingsPanel
      title="Edit Currency"
      description="Configure the default currency and exchange rates for your agency."
    />
  );
}