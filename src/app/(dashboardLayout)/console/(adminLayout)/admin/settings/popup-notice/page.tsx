import SettingsPanel from "../../_components/Settings/SettingsPanel";

export default function PopupNoticePage() {
  return (
    <SettingsPanel
      title="Pop-up Notice"
      description="Configure the pop-up announcements shown to your partners on login."
    />
  );
}